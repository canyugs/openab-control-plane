* _2026-09-09 13:48:21 (GPT-6/default)_

# Stage 2 discovery resolution

Source map accepted with corrections, not a new source-work authorization. Worker changed only codewalk-report.md. Its claim that prior requirements are outside the clone is incorrect: tracked hidden .agentflow paths exist in the clone; the brief's relative shorthand plus default rg hidden-file exclusion caused a lookup miss. Host read the exact prior requirements and mandatory resolution. Spec must read their full repository-relative paths; no repeated paid codewalk is needed for this access-path correction.

Requirements R-13..R-18 remain accepted with the human-quality correction. Source evidence independently checked: controller audit timestamps are milliseconds; product rows seconds; done_at is set separately after provider-success audit; real external ingress uses action.accepted and action.completed correlation, while ingress.accepted is plan-only. Therefore do NOT use ingress.accepted as the active cohort's start. Follow delivery/action to session uniquely, dedupe retries at session scope; ambiguity is unknown.

Reject the report's suggestion that a terminal failed GitHub write is visible failure. It proves delivery failure, not public visibility. Visible failure requires successful/reconciled diagnostic/error/tombstone receipts; supersession remains separate even if its tombstone is visible. No public receipt means incomplete/unknown, never visible failure.

Expected required writes must come from explicit immutable target/round outcome and actual terminal branch, cross-checked with enqueues; do not infer completeness solely from whichever writes happen to exist. Include scoped round/target evidence in the offline bundle. Use source-backed conservative sets for normal verified review, diagnostic failure and explicit supersession; no owner choice is needed to copy current behavior. Unknown/missing evidence stays unknown.

Default local operator report, Taipei ISO weeks, Markdown+JSON, no network/scheduler/new DB. Human annotations join exact session/finding identity (not stable_id alone); no dismissal/waive inference. Actual cost requires provider-reconciled per-session attribution and currency, not broad account totals or estimates. Report observed latency and reconciliation timing limits honestly; no exact earlier provider timestamp invention. Tests should be report-local fixture tests, not unrelated full Rust checks for a future script-only change.

Explore/spike skipped: explicit offline fixture contract can encode unknowns and needs no runtime experiment to design. Security/acceptance of any source implementation follow its future exact design approval. New external spec is the next step, with exact paths and all corrections.

Self-check: Source map checked; missing-path claim and classification errors corrected; source authority remains gated.
