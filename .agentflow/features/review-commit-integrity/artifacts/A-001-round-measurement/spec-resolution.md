* _2026-09-09 13:57:13 (GPT-6/default)_

# Stage 2 specification sign-off

Spec sign-off: PASS with the following mandatory source-backed clarifications. The frozen design comprises design.md AND this resolution; prior spec-report.md is superseded. External spec stage used two starts, not a new review stage. Full final report read, digest matched and clone changed only the declared report. No Stage 2 implementation or tests are claimed.

- Cohort acceptance means kind action.accepted AND outcome accepted (lib.rs:845-882), not outcome succeeded. Exclude target.reason ask, keep plan-only out. Use earliest accepted event for each uniquely resolved session, with week assignment after session deduplication across captured evidence; unmatched actions retain their own unknown bucket. Missing session/target cannot prove an ask exclusion. Accepted denominator and exclusions/unknowns remain explicit.
- The required diagnostic set is source-derived: comment ALWAYS, plus status:error iff immutable target SHA is valid full 40 ASCII hex. A missing error-status enqueue for a valid target is incomplete, even if every observed comment succeeded. Normal verified always requires comment + correct status + correct formal review. Author-decision and opening-comment writes are not terminal-round requirements. Legacy_unverified is not itself proof that a diagnostic was planned; absent supported terminal evidence stays unknown. This retains the original R-16 completeness rule and closes the corrected draft's ambiguous 'actual planned' wording.
- Coverage is metric-specific. Missing human/cost input yields unknown quality/cost, not unknown reliability. Reliability requires complete relevant audit/product coverage and valid required receipts; all zero-valued categories and explicit denominators remain in both outputs.
- Use exact source column units: audit occurred_at/recorded_at are ms; controller created_at/claimed_at/done_at/received_at are seconds. runtime_event_receipts.occurred_at is copied from the runtime envelope, not converted by the store (sqlite.rs:450-480); preserve it and do not treat all product timestamp columns as seconds. Runtime receipt admission time received_at supplies the snapshot cutoff for supersession/timeout evidence; the report need not use runtime occurred_at for latency.
- product.json is an object mapping the six listed table names to arrays of raw rows. Manifest version is review-round-weekly-evidence/v1; each source declares coverage and capture span; unknown evidence cannot be filled from present PR state. Required capture columns and JSON/API page shape remain those of the final report. Cross-source joins that cannot be established at the snapshot cutoff are unknown. File hashes prove byte identity only.
- Human annotations require explicit annotator identity, timestamp, version, full finding/session scope, verdict and evidence reference. Escapes require confirming human identity/time/version, reviewed-window identity and evidence reference. Conflicting natural identities are unknown, not last-write-wins. Filter late human/cost observations at snapshot_at. Cost currency and minor-unit definition must agree before a same-currency sum; any partial attempt coverage leaves total unknown.
- CLI validation uses bounded UTF-8 JSON, rejects unsupported manifest version/hash/path corruption and input/output overlap or overwrite. Exact duplicates dedupe, conflicting evidence produces counted unknowns. Preserve useful bounded escaped repo/PR/session/row identifiers; omit bodies/credentials/free-text errors. No generic privacy or export subsystem.

The three future paths, offline standard-library route, R-13..R-18/INV-7 coverage and local fixture/PTY validation plan are accepted. Conservative Taipei ISO-week/manual-report defaults need no new owner preference. Implementation starts only after exact Design Go for the commit containing this frozen design and resolution. Stage 1 Result Go d6e96c3 does not authorize Stage 2 source work or production rollout.

Self-check: Final source facts and original requirements reconciled; no new product scope or execution approval inferred.
