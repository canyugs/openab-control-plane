* _2026-09-09 11:53:19 (GPT-6/default)_

# Requirements host resolution

Accepted for design with mandatory corrections: R-13 human finding validity/usefulness and confirmed escapes are the quality signal; structural SHA/reviewer gates are integrity/reliability, never quality substitutes. R-19/R-22 compare two separate arms: existing council vs independently prompted reviewers followed by exactly one synthesis over THAT arm only. Never synthesize both arms together. R-21 no new comparison database/service is justified; immutable local artifacts and input manifests are the smallest baseline. R-4 fail closed for both approval and request_changes on unverified SHA: error/diagnostic only, no findings import or waiver firing. Full 40-hex GitHub SHA is the current policy; reject abbreviation/placeholders, compare normalized hex without inventing missing evidence.

These are clarifications of the accepted owner outcomes, not new product scope. Requirements sign-off: PASS with these corrections, auto-reply on applies routine defaults only. Design Go for an exact commit is still pending. Artifact inspected in full; worker exit 0 and clone changed only requirements-report.md. Source evidence independently inspected in closing.rs, github.rs, deciding.rs and lib.rs.

Self-check: Requirements accepted only with recorded corrections; no source or runtime changes claimed.
