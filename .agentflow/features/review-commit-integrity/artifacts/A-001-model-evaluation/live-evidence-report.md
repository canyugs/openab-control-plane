* _2026-09-10 00:57:37 (gpt-5.6-luna/max)_

Implemented the scoped E3/E5/E6 live-evidence corrections at source commit `ea94f2c72fadad02dfae2705369b81acc94bb9cc`.

Authority and frozen context:

- Mode: full_pipeline implementation; tier basic; configured codex-default `gpt-5.6-luna/max`.
- Owner Design Go: `1107f73567710159c298ff6a60df393e8e6b9775`.
- Source authority: `scripts/review_model_evaluation.py`, limited to discovery validation, structural source-binding helpers, and discovery/validation packet and role instructions.
- No live model, authentication, network, Docker, or generated-source execution was used.

Changes:

- Discovery now requires every evidence ID to exist and requires at least one declared evidence range to cover the candidate source location. Related cross-file contract evidence is allowed. Citation validation remains per-citation and still rejects mismatched, unknown, and out-of-range evidence.
- Blind discovery instructions now require actionable defects directly proven by the supplied source under a supplied contract or actual call paths; style, documentation, type, and speculative missing-caller assumptions are excluded, and an empty result is required when no concrete defect is proven. Original findings and expected findings/counts remain withheld.
- Structural Python source binding recognizes an exact `/source/...` path bound to a name used by ordinary `open`/read calls, and recognizes `importlib.util.spec_from_file_location` source loading. AST parsing remains only a structural signal, not semantic proof. Print-only, path-only, comment-only, argv-echo, and malformed/doubly escaped controls remain unbound.
- Validation instructions clarify that decoded `utf8` is raw source text with actual newlines and JSON escaping occurs once. Generated bytes are not unescaped or rewritten; malformed or doubly escaped source remains unproven.

Evidence and red-first result:

- Read `generation-discovery-live.json`, `structured-output-retry-envelope.json`, and `correction-contract.md`. The captured discovery candidate uses `sample.py` lines 1–2 with `E_SAMPLE` plus cross-file `E_CONTRACT` on `README.md` line 1. The captured validation envelope contains the `SRC`/`open(SRC, ...)` pattern and JSON-escaped generated UTF-8.
- Added `tests/test_review_model_live_evidence.py` and preserved its tests-only red patch in `red-correction.patch` (test-file hash `31992ec15b44e903ad15a90a79394d14518a78f3`). Before the source correction, the focused run exercised 7 tests and produced the expected 3 failures plus 1 validation error for the missing behaviors.

Verification:

- Focused live-evidence regressions: 7 passed.
- Controller regressions: 25 passed.
- Adapter/auth regressions: 3 passed.
- OCI regressions: 16 passed.
- Full Python test suite: 68 passed.
- `git diff --check`: passed.
- Final tracked/untracked scope contains only the authorized source change, new test, `red-correction.patch`, and this report.

The independent two-judge meaningful-validation gate and existing negative semantic controls were retained and passed through the controller suite. No live acceptance result is claimed; parent acceptance and import remain outstanding.

Self-check: Exact source, authority, scope, model, effort, result and evidence contract frozen.
