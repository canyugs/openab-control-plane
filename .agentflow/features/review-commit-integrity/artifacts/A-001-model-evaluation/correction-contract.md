* _2026-09-09 23:13:59 (GPT-6/default)_

# Bounded correction integration contract

Source candidate ff9fc04 passed 18 host tests but is not accepted. Existing E-1..E-12 require the runtime/source corrections below. No feature scope expansion, no new owner decision, no skipped stages. Three external implementation workers own disjoint files. All public function signatures remain compatible except additive fields/helper below; coordinator integrates then runs all relevant tests and live acceptance.

- Core worker owns review_model_adapters.py, review_model_evaluation.py, test_review_model_evaluation.py, fixtures/model_evaluation, docs/model-evaluation.md. OCI worker owns review_model_oci_executor.py and test_review_model_oci_executor.py. Weekly worker owns review_round_weekly_report.py, its test module and docs/review-round-weekly-report.md. None edits another worker's paths.
- OCIExecutor.execute retains signature and returns raw observations, plan, runs and provenance. Add controls_passed: bool (both actual exits zero, both bounded structured observations match expectations, and meaningful declared claim_present booleans differ between baseline/counterexample). Add claim_present: bool|null for baseline. Raw OCI classification stays unproven until independent assessment; status may be success for controls_passed, but execution alone cannot prove source semantics. runs retain bounded raw stdout and stderr plus digests, output_limit/timeout flags. A metadata-only stdout difference is insufficient. Source-relevance checks in model pipeline must not use source-path substring as proof.
- Core evaluates validation first, then sends the same complete source packet plus generated plan and observed OCI controls to both fresh judges, with no peer outputs. Add judge validation_verdict: valid|invalid|unproven to assess whether the generated controls actually exercise the cited source and distinguish the claim; existing counterexample/citations explain judgment. Both valid assessments, nonblocked passed controls, matching supported/refuted claim observation and nonconflicting synthesis are needed for executed_reproduced/executed_refuted and scoreability. Model disagreement remains visible and results remain model_assessment, never ground truth. Pure constants, source paths in comments/strings, assert-false and failed controls remain unproven. The machine cannot establish arbitrary program semantics from substring matching; independent model assessment is the stated qualification.
- Core exports verify_evaluation_artifacts(root: Path) -> dict returning summary only after read-only validation of schema/state/input identity and every run.json artifact_hashes entry. run.json.artifact_hashes is a map from safe relative regular-file paths to SHA256, including snapshot/source packet/findings/omissions/summary.json/summary.md and all complete invocation/validation evidence; run.json is the ledger and excluded from self-hashing. Validate symlink/traversal/missing/changed/conflicting files. Both resume and weekly use this helper. Partial/failed outputs retain explicit state and no quality score; successfully completed invocation AND validation artifacts are reused without new model/container execution. Preserve incomplete attempt history/actual captures, never erase evidence to make resume work.
- Weekly reads evaluated summaries through that helper (core implements it concurrently). It never calls model/OCI or changes evaluation files. Missing helper while integrating is a dependency, not permission to trust raw summary JSON. Tests may patch the helper for unit coverage but integrated tests must use a real hash-bound evaluation fixture. Report identity incorporates exact evaluation identities/hashes, so adding/changing evaluations cannot reuse a previous report ID. Duplicate roots/identities deduplicate; conflicts become unknown/error.
- OCI default pinned image becomes the actually pulled/tested python@sha256:b64631e04e4920160c50fbe8d8df828f7f35f06f425cb44aa09bca53e708a35a, with no automatic unpinned fallback. Generated plans still use literal arrays/files only, materialized only in OCI. Core docs state image/runtime prerequisites; no manual check authoring requirement.
- Verified OAuth text-only transport and real event-array/cost semantics are in transport-preflight-resolution.md and transport-preflight.json. Core supports it without weakening executable/file/MCP tool disabling. No credential export or user-written adapter.

Source-backed weekly expected positive control and OCI negative controls are in early-runtime-controls.json. All corrective tests start red against ff9fc04 before code changes. Parent owns actual authenticated model and Docker project-CLI PTY acceptance. Three worker stages are implementation subdivisions, not independent review attempts. Security/acceptance remain fresh later gates.

Self-check: Existing functional outcomes mapped to bounded parallel corrections and explicit integration seams.
