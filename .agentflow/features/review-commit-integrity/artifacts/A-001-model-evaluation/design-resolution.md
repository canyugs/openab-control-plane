* _2026-09-09 21:13:28 (GPT-6/default)_

# Capability-first design acceptance

Requirements sign-off A: PASS. E-1..E-12 trace to owner requests for other-model synthesis, acceptance of added work/cost, and explicit functionality before savings. Human input is optional. First delivery must actually call distinct models, synthesize, discover omissions, independently assess candidates, generate and execute tests in isolation, and produce report artifacts. Neither hand-authored checks nor user-written model adapters are prerequisites. Existing R-14..R-18 source/clock/receipt/report corrections remain authoritative where not superseded.

Specification sign-off B: PASS with the following implementation constraints. This resolution and design.md form the frozen design together. Host read the full final report, matched external-runner SHA-256 and verified report-only clone writes. The amended design stage used two worker starts. No source implementation or live model/OCI journey is claimed.

- Local Codex and Claude --help independently confirm the displayed command families/flags. Claude --json-schema takes literal JSON schema text, not a filename. The adapter must pass parsed/serialized schema bytes as one argv value, use the actual JSON/structured output that the installed CLI emits (probe and real smoke), and retain transport metadata separately from model-authored content. Command examples are executable-adapter requirements, not proof that output parsing already works.
- Codex --sandbox read-only and an empty directory alone do not meet the no-host-model-command/no-credential-read boundary. An enabled Codex adapter must prove enforced tool disabling or equivalent verified process confinement before handling source packets; instructions to a model are insufficient. No unsafe fallback is allowed. The inspected Claude --bare --tools empty --strict-mcp-config path provides a concrete text-only adapter; two distinct strong configured Claude IDs can exercise the complete workflow if Codex confinement is unavailable. Adapter capability status is explicit; all evaluation/discovery/test/synthesis stages still run. This is a real supported transport choice, not a cost shortcut or an invented wrapper prerequisite. Profile selection and real acceptance must use actual available authenticated models, without silently changing IDs.
- All generated executable content is materialized and executed only in the isolated OCI test workspace. It must not reach orchestration-host shell, provider CLI tools, project hooks or original checkout. Evidence/control adjudication distinguishes a reproduced code behavior from a failing generated harness. Generated test files plus actual observed outputs remain in raw result artifacts for inspection; source references alone prove neither truth nor execution.
- Distinct judge roles are fresh and cannot read each other's output; synthesis is a fresh third-role invocation, with anonymized judge labels and preserved disagreement. Original findings and omission candidates both receive the required independent assessment and generated-validation chain. Discovery sees no original findings; same model family/alias uncertainty remains visible, not misrepresented as fully independent truth.
- The ordinary run can complete without human annotations or hand-authored checks. Environment/auth/model profiles are operational prerequisites, not a request to implement missing product code. Container daemon setup is part of later local validation work; current Docker executable exists but daemon readback failed, so no container-ready claim. Exact source/archive packet completeness and context-limit errors must be explicit; no source truncation masquerades as a complete evaluation.
- First acceptance includes deterministic tests AND a real project-CLI PTY journey on synthetic vulnerable/safe snapshots using actual distinct configured models, generated positive/negative controls and running OCI execution, plus report readback. Mock success or environment-blocked-only runs cannot satisfy full capability acceptance. Costs/usage are observed with actual-versus-unknown distinction; no cheap substitutions/stage skipping for savings. Code and runtime setup remain pending the new exact Design Go.

The exact future file list in design.md is accepted; three-file restriction at old Stage 2 commit 5f18484 is superseded. Stage 1 Result Go d6e96c3 remains valid and unchanged. No merge, production GitHub writes, automatic verdict authority, fleet or Stage 3 experiment is inferred. No further generic permission to spend is needed; owner already approved that dimension.

Self-check: Functional owner outcome and concrete transport/execution constraints accepted; identities/proof checked; design gate remains exact and source work not claimed.
