* _2026-09-10 18:18:14 (GPT-6/default)_

# Linux runtime validation

Source: a46a93845c0f819362b5f08f07345ffa67cbb69f; accepted packaging implementation ab483e48e99995c90ea2b51bec3432920461a758.
Image: sha256:38a45068982b62ad6999173f358b5f468f7e34f06f5dae9f4dd938a94012ab0b (linux/amd64 under Docker Desktop emulation on arm64).

The approved next action was a fresh real-model Linux evaluation before publication. Existing Keychain OAuth was read only into process memory, passed to Docker by an environment name, and never put in argv, files, reports or image layers. The auth probe returned LINUX_AUTH_OK with actual Opus5 provider usage, exit0, without socket/source/tool access.

The host runtime driver launched the unchanged installed evaluator using the accepted nonroot UID, socket GID0, identical scratch paths and dedicated readonly source staging contract. It used direct Docker invocation to avoid writing a credential file; the packaged launcher was unchanged. The source is the accepted synthetic safe fixture, not a production PR: revision b7927775d4db77f360efe4d96a9b82fc61b7b35c and base4ed638bb12f3e1fa11be2524a48e18973ad4591f.

Fresh evaluation: exit0, statecomplete,130.482s. Two model judgments refuted the original finding; generated baseline/counterexample OCI execution confirmed executed_refuted. Synthesis completed and discovery completed with no candidates. Provider modelUsage records requested Opus5 and Opus4.6 usage, including auxiliary Haiku usage. The result-only JSON envelopes contain no assistant model events, so observed_model_id remains unavailable. This is an explicit precision limit, not a model-auth failure or proof of a substituted model. No semantics or identity parser was changed.

Estimated list-price evaluation cost is USD0.9049775; actual billing remains unknown. No human accuracy, recall, production-quality or billing guarantee follows from the synthetic test.

Complete replay with all supported auth variables removed exited0 and retained every hash and mtime for 42 evaluation files. Network-none image weekly generation exited0, report_id ab9d8019d18f515826cc1cfeb2c927a5f6a0a269f29aadb43b9b20465c6f0476. Original fixture repository and42 earlier accepted artifacts are unchanged.

The readback helper's final empty-scratch assertion initially failed after all replay/hash/weekly/source checks had passed: Claude CLI left one empty claude-501 directory. Parent independently checked it contained no files and removed only that empty task-owned directory using rmdir. Scratch is empty after cleanup; this observation is retained, not hidden as an all-checks-pass claim. The package promises bounded staging cleanup, not removal of all CLI temp directories.

Raw proof: auth-probe.json, run-start.json, run-result.json, readback.json, pty.json, evaluation/ and weekly/. Credential-leak scan passed before evidence copying. PTY /dev/ttys004 closedexit0. No product files changed, so previous source tests/review remain current; no redundant source suite or paid review was run for operational records. No release tag, merge, registry publication or production deployment occurred.

Self-check: Actual runtime, identity and billing limits, helper failure, scoped cleanup and unchanged source/evidence verified.
