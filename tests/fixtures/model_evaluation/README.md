# Model-evaluation fixtures

These are offline input fixtures only. They contain no credentials, live
provider claims, or executable host-generated files. The Claude event-array
file is a synthetic envelope shape used to exercise transport parsing and
list-price estimate accounting. Tests provide fake transports and OCI
observations around them.
