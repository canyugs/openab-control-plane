# Model-evaluation fixtures

These are offline input fixtures only. They contain no credentials, provider
responses that claim live success, or executable host-generated files. Tests
provide fake transports and OCI observations around them.
