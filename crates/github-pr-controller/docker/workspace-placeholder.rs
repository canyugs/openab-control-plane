//! Placeholder root target used only while validating the workspace lockfile
//! in the independent controller image. OCP implementation source is not
//! copied into that build context.

#![forbid(unsafe_code)]
